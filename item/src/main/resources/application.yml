server:
  port: 8080

spring:
  application:
    name: custom_module
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  flyway:
    enabled: true
    locations: classpath:/db/migration

response:
  result:
    code: 200
    msg: 成功